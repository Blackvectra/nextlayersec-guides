# YARA Rules

YARA rules for file and memory matching. Use [`_template.yar`](_template.yar) as the starting point.

Pair each `.yar` with a `.md` sibling describing the malware family, indicators, and tested samples (hash references only — never commit samples).
