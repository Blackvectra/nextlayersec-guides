# Python dependencies used by the CI workflows in .github/workflows/.
#
# This file exists so Dependabot can monitor and PR upgrades to these
# packages. lint.yml installs from this file rather than inline pip
# install statements — that way the version constraints live in one place
# and Dependabot can act on them.
#
# If you add a new pip-installed tool to a CI workflow, add it here too.

pysigma>=1.3,<2
zizmor>=1.25.2,<2
